# Maintainer: NuGet publish (not shipped in the package)

Trusted Publishing on nuget.org → GitHub:

| Field | Value |
|-------|--------|
| Owner | `AI-Guiders` |
| Repository | `roslyn-mcp-core` |
| Workflow file | `publish-nuget.yml` |
| NuGet user | `LonelySoul` |

Release: tag `vX.Y.Z` or Actions → **Publish NuGet** → workflow_dispatch with SemVer `X.Y.Z` (no `v` prefix).
